"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const endpoint_1 = require("./endpoint");
const http_1 = require("./http");
const url_1 = require("url");
class Client {
    constructor(config) {
        if (!config.APP_KEY) {
            throw new Error("no APP_KEY");
        }
        this.config = config;
        this.http = new http_1.default({
            baseURL: `${endpoint_1.WHATSAPP_BASE_API}/sm/api/v1`,
            defaultHeaders: {
                "apikey": config.APP_KEY,
                "Content-Type": "application/x-www-form-urlencoded",
                "Accept": "application/json",
            },
        });
    }
    sendText(source, destination, sourceName, message) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "text", "text": "${message}" }`,
        }));
    }
    sendAudio(source, destination, sourceName, url) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "audio", "url": "${url}" }`,
        }));
    }
    sendDocument(source, destination, sourceName, url, filename) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "file", "url": "${url}", "filename": "${filename}" }`,
        }));
    }
    sendImage(source, destination, sourceName, originalUrl, previewUrl, caption) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "image", "originalUrl": "${originalUrl}", "previewUrl": "${previewUrl}", "caption": "${caption}" }`,
        }));
    }
    sendLocation(source, destination, sourceName, longitude, latitude, name, address) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "location", "longitude": ${longitude}, "latitude": ${latitude}, "name": "${name}", "address": "${address}" }`,
        }));
    }
    sendSticker(source, destination, sourceName, url) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "sticker", "url": "${url}" }`,
        }));
    }
    sendVideo(source, destination, sourceName, url) {
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "video", "url": "${url}" }`,
        }));
    }
    sendReplyButtons(source, destination, sourceName, payload, button) {
        let resultContent = '';
        let resultOptions = '';
        let entriesContent = Object.entries(payload.content);
        entriesContent.map(([key, val], i, arr) => {
            let message = resultContent;
            if (arr.length - 1 === i) {
                resultContent = message.concat(`"${key}": "${val}" `);
            }
            else {
                resultContent = message.concat(`"${key}": "${val}", `);
            }
        });
        let entriesOptions = button.options;
        entriesOptions.map((e, i, arr) => {
            let message = resultOptions;
            if (arr.length - 1 === i) {
                resultOptions = message.concat(`{"type": "${e.type}", "title": "${e.title}"} `);
            }
            else {
                resultOptions = message.concat(`{"type": "${e.type}", "title": "${e.title}"}, `);
            }
        });
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "quick_reply", "content": { ${resultContent} }, "options": [ ${resultOptions} ] }`
        }));
    }
    sendListMessage(source, destination, sourceName, payload) {
        let resultItems = '';
        let resultButtons = '';
        let entriesItem = payload.items;
        entriesItem.map((e, i, arr) => {
            let message = resultItems;
            let resultOptions = '';
            let entriesOption = e.options;
            entriesOption.map((option, j, array) => {
                let messageOption = resultOptions;
                if (array.length - 1 === j) {
                    resultOptions = messageOption.concat(`{"type": "${option.type}", "title": "${option.title}", "description": "${option.description}"} `);
                }
                else {
                    resultOptions = messageOption.concat(`{"type": "${option.type}", "title": "${option.title}", "description": "${option.description}"}, `);
                }
            });
            if (arr.length - 1 === i) {
                resultItems = message.concat(`{"title": "${e.title}", "subtitle": "${e.subtitle}", "options": [ ${resultOptions}] }`);
            }
            else {
                resultItems = message.concat(`{"title": "${e.title}", "subtitle": "${e.subtitle}", "options": [ ${resultOptions}]}, `);
            }
        });
        let entriesButton = payload.globalButtons;
        entriesButton.map((e, i, arr) => {
            let message = resultButtons;
            if (arr.length - 1 === i) {
                resultButtons = message.concat(`{"type": "${e.type}", "title": "${e.title}"} `);
            }
            else {
                resultButtons = message.concat(`{"type": "${e.type}", "title": "${e.title}"}, `);
            }
        });
        let test = `{ "type": "list", "body": "${payload.body}" , "title": "${payload.title}", "globalButtons" : [ ${resultButtons}], "items" : [ ${resultItems}]  }`;
        console.log(test);
        return this.http.post("/msg", new url_1.URLSearchParams({
            channel: "whatsapp",
            source,
            destination,
            "src.name": sourceName,
            message: `{ "type": "list", "title": "${payload.title}", "body": "${payload.body}", "globalButtons" : [ ${resultButtons}], "items" : [ ${resultItems}]  }`
        }));
    }
}
exports.default = Client;
